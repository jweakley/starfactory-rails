%section.textual
  %h1 Events
  %ul.tiles
    - @events.each do |event|
      %li
        = link_to event do
          %h3= event.workshop_name
          %time= event.starts_at.strftime('%b %e, %Y %l:%M %P')
